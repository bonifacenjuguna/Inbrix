{
  "name": "inbrix-bot",
  "version": "0.1.1",
  "description": "Inbrix — owner-only Telegram bot that forwards Gmail into Telegram (poll + push together).",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/db/migrate.js",
    "smoke-test": "node scripts/smoke-test.js",
    "get-refresh-token": "node scripts/get-refresh-token.js",
    "get-refresh-token-prod": "node scripts/get-refresh-token-prod.js",
    "setup-watch": "node scripts/setup-gmail-watch.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.1",
    "ioredis": "^5.4.1",
    "node-cron": "^3.0.3",
    "node-html-parser": "^6.1.13",
    "pg": "^8.12.0",
    "telegraf": "^4.16.3"
  }
}
